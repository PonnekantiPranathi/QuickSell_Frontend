import React from 'react';
import '../styles/page.css';  // Shared CSS for page layout

const PriorityOrdering = () => {
    return (
        <div className="page-container">
          <h1>Display Priority Ordering Page</h1>
        </div>
      );
};

export default PriorityOrdering;
